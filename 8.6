package com.company;

import java.util.ArrayList;
import java.util.Random;

public class Main {

	public static void main(String[] args) {
		Array_List array = new Array_List();
		array.insertElement(4);
		array.insertElement(5);
		array.insertElement(6);
		array.insertElement(7);

		array.printElements();

		System.out.println();


		array.printElements();
		
	}
}

public class Array_List {
    ArrayList<Integer> elements = new ArrayList<>();

    void insertElement(int element) {
        elements.add(0, element);
    }

    void printElements() {
        for (int i = 0; i < elements.size(); i++) {
            System.out.println(elements.get(i));
        }
    }

    void retrieveEllement(int index) {
        System.out.println(elements.get(index));
    }

    void removeThirdElement() {
        elements.remove(2);
    }


    ArrayList<Integer> reverseArrayList(ArrayList<Integer> alist) {
        ArrayList<Integer> revArrayList = new ArrayList<Integer>();
        for (int i = alist.size() - 1; i >= 0; i--) {
            revArrayList.add(alist.get(i));
        }
        return revArrayList;
    }


}
